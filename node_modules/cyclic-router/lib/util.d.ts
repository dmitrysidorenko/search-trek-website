import { Pathname } from '@cycle/history/lib/interfaces';
declare function splitPath(path: Pathname): Pathname[];
declare function filterPath(pathParts: Pathname[], namespace: Pathname[]): Pathname;
declare function makeCreateHref(namespace: Pathname[], _createHref: (pathname: Pathname) => Pathname): (path: string) => string;
export { splitPath, filterPath, makeCreateHref };
